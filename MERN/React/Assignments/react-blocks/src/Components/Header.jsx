import React from "react";

export const Header = (props) => {

    const styles = {
        width: "100%",
        height: "100px",
        backgroundColor: "green",
        display: "block",
    };

    return <span style={styles}></span>;
}